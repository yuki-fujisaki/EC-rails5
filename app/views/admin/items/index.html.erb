<% @items.each do |i| %>
<div>
  <%= i.id %>
  <%= i.name %>
  <%= i.tax_excluded_price %>
  <%= i.genre.name %>
  <%if i.is_active %>
   販売中
  <% else %>
   販売停止中
  <% end %>
</div>
<% end %>
<%= link_to '商品新規登録', new_admin_item_path %>
